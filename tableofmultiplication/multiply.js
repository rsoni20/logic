function calculate(){
    
    for (var counter = 1; counter<=10; counter++){
        
      document.write("12 * " + counter + " "+ "--"+counter*12+"<br>") 
    }
    // document.write("12 * " + counter + " = " + conter*12);
}

 